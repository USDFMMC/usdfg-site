[build]
  command = "npm install && npm run build"
  publish = "dist"            # use "dist/public" if your vite.config writes there

[build.environment]
  NODE_VERSION = "20"
  ROLLUP_DISABLE_NATIVE = "1"
