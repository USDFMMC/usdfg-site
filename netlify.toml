[build]
  base = "client"
  command = "npm install --force && npm run build"
  publish = "../dist" # ✅ match outDir in vite.config.ts

[build.environment]
  NODE_VERSION = "20"
  ROLLUP_DISABLE_NATIVE = "1"
